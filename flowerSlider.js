const carouselSlider = document.querySelector(".carouselSlider");
const carsouselImages = document.querySelectorAll(".carouselSlider img");

//Counter
let counter = 1;
const size = carsouselImages[0].clientWidth;
console.log(size);
